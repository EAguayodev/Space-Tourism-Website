{
  "dependencies": {
    "@tailwindcss/cli": "^4.2.2",
    "tailwindcss": "^4.2.2"
  },

  "devDependencies": {
    "typescript": "^6.0.2"
  },

 "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsc --w -p tsconfig.json"
  }
}
